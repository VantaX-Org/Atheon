{
  "name": "atheon-agent",
  "version": "1.0.0",
  "description": "Atheon hybrid deployment agent sidecar — heartbeat, config sync, self-update",
  "private": true,
  "scripts": {
    "start": "node dist/agent.js",
    "build": "esbuild src/agent.ts --bundle --platform=node --target=node20 --outfile=dist/agent.js",
    "dev": "tsx src/agent.ts"
  },
  "dependencies": {
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.11.0"
  }
}
